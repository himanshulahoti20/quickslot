import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary     = Color(0xFF00C853); // sporty green
  static const Color primaryDark = Color(0xFF00E676); // brighter for dark mode

  // Slot status
  static const Color available = Color(0xFF00C853);
  static const Color booked    = Color(0xFF90A4AE);

  // Light surfaces
  static const Color surfaceLight    = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color cardLight       = Color(0xFFFFFFFF);

  // Dark surfaces
  static const Color surfaceDark    = Color(0xFF1E1E1E);
  static const Color backgroundDark = Color(0xFF121212);
  static const Color cardDark       = Color(0xFF2C2C2C);

  // Text — light
  static const Color textPrimaryLight   = Color(0xFF212121);
  static const Color textSecondaryLight = Color(0xFF757575);

  // Text — dark
  static const Color textPrimaryDark   = Color(0xFFEEEEEE);
  static const Color textSecondaryDark = Color(0xFFBDBDBD);
}
